dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.7.3
  url_launcher: ^6.3.2
  torch_light: ^1.1.0
  http: ^1.6.0
  get_storage: ^2.1.1
  # shared_preferences: ^2.5.5  <-- (Hapus atau beri tanda pagar jika tidak dipakai)
  audioplayers: ^6.7.1
  camera: ^0.12.0+1
  permission_handler: ^12.0.3
  flutter_compass: ^0.8.1
  sensors_plus: ^6.1.1
  supabase_flutter: ^2.14.1
  flutter_dotenv: ^5.1.0
  google_sign_in: ^6.2.1
  
  # === TAMBAHAN UNTUK PERFORMA & UI ===
  cached_network_image: ^3.3.1   # Untuk cache foto profil
  flutter_image_compress: ^2.2.0 # Untuk kompres foto semaphore sebelum dikirim ke API
  image_picker: ^1.0.7           # Untuk ambil foto profil/galeri
  flutter_svg: ^2.0.10           # (Opsional) Jika kamu pakai gambar vector .svg untuk logo

# ... (bagian lain tetap sama) ...

flutter:
  uses-material-design: true

  fonts:
  # ... (Konfigurasi font Urbanist kamu biarkan seperti semula) ...

  assets:
    # === TAMBAHAN FOLDER ASSET UMUM ===
    - assets/images/       # Untuk menampung logo.png, ilustrasi_login.png, dll
    - assets/icons/        # (Opsional) jika punya icon custom
    
    # Asset bawaan kamu:
    - assets/images/kotak1/
    - assets/images/kotak2/
    - assets/images/huruf_semaphore/
    - assets/images/image_sandi/
    - assets/images/games/
    - assets/sounds/
    - .env